---
phase: 08-dual-channel-notification-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/email/types.ts
  - src/lib/email/templates.ts
  - src/lib/email/templates.test.ts
  - src/lib/email/service.server.ts
  - src/lib/email/service.test.ts
autonomous: true
requirements:
  - EMAIL-01
  - EMAIL-02
  - EMAIL-03
  - EMAIL-04
  - EMAIL-05
  - EMAIL-06
  - EMAIL-07
  - EMAIL-08
user_setup: []

must_haves:
  truths:
    - "System generates responsive Indonesian HTML and plaintext transactional email templates for booking submission confirmation, admin operational alerts, approval confirmations, and rejection notices with mandatory reasons."
    - "Dates and times in email templates are rendered in Asia/Jakarta (WIB) timezone with correct wall-clock time formatting and tracking links."
    - "System validates and sanitizes recipient email addresses (single or comma-separated) and handles malformed addresses gracefully without throwing errors."
    - "When RESEND_API_KEY is not configured or in mock/test mode, the system outputs structured console ASCII mock boxes and returns simulated successful dispatches."
    - "Outbound transactional email attempts record immutable audit logs with channel 'notification.email_dispatch' containing recipient, template, provider, and status metadata."
  artifacts:
    - src/lib/email/types.ts
    - src/lib/email/templates.ts
    - src/lib/email/service.server.ts
    - src/lib/email/templates.test.ts
    - src/lib/email/service.test.ts
  key_links:
    - "src/lib/email/service.server.ts uses templates from src/lib/email/templates.ts and records audit logs via src/lib/audit/audit.server.ts"
    - "src/lib/email/templates.ts uses Asia/Jakarta timezone utilities from src/lib/timezone/datetime.ts"
---

<objective>
Implement the Resend Email Gateway integration service, email validation and sanitization utility, responsive Indonesian HTML and plaintext template engine, ASCII console mock fallback provider, and channel-specific audit logger.

Purpose: Provide a reliable, decoupled, and secure foundation for delivering transactional emails without blocking application database transactions.
Output: `src/lib/email/types.ts`, `src/lib/email/templates.ts`, `src/lib/email/service.server.ts`, and test suites.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dual-channel-notification-integration/08-CONTEXT.md
@.planning/phases/08-dual-channel-notification-integration/08-RESEARCH.md
@src/lib/audit/audit.server.ts
@src/lib/timezone/datetime.ts
@src/lib/whatsapp/service.server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Responsive HTML & Plaintext Email Templates Engine</name>
  <files>src/lib/email/types.ts, src/lib/email/templates.ts, src/lib/email/templates.test.ts</files>
  <read_first>src/lib/timezone/datetime.ts, src/lib/whatsapp/templates.ts, .planning/phases/08-dual-channel-notification-integration/08-CONTEXT.md, .planning/phases/08-dual-channel-notification-integration/08-RESEARCH.md</read_first>
  <action>
1. Create `src/lib/email/types.ts` defining:
   - `EmailTemplateType`: `"BOOKING_SUBMITTED_REQUESTER" | "BOOKING_SUBMITTED_ADMIN" | "BOOKING_APPROVED" | "BOOKING_REJECTED" | "BOOKING_CANCELLED"`
   - `EmailDispatchResult`: `{ success: boolean; mock?: boolean; messageId?: string; error?: string; rawResponse?: unknown }`
   - `SendEmailParams`: `{ to: string | string[]; subject: string; html: string; text: string; bookingId?: string; templateType?: EmailTemplateType }`
   - Template data interfaces: `BookingSubmissionRequesterEmailData`, `BookingSubmissionAdminEmailData`, `BookingApprovalEmailData`, `BookingRejectionEmailData`, `BookingCancellationEmailData`.
2. Create `src/lib/email/templates.ts` with responsive HTML table layout and plaintext counterparts:
   - Base HTML layout wrapper: 600px container, `#f8fafc` outer background, `#ffffff` card, `#1e3a8a` navy branding, header with "SARPRAS PPKASN", status badge, content body, primary CTA button, footer with institutional text.
   - `buildBookingSubmissionRequesterEmail(data)`: Requester confirmation email with ref code, asset details, Asia/Jakarta (WIB) schedule, purpose, and "Cek Status Permohonan" CTA link (EMAIL-05).
   - `buildBookingSubmissionAdminEmail(data)`: Admin operational alert email with ref code, requester name, organization, attendance, schedule, purpose, and "Tinjau di Panel Admin" CTA link (EMAIL-06).
   - `buildBookingApprovalEmail(data)`: Approval notification email with asset location, schedule confirmation, guidelines, and "Lihat Rincian Booking" CTA link (EMAIL-07).
   - `buildBookingRejectionEmail(data)`: Rejection notification email with ref code, schedule, highlighted rejection reason box, and tracking link (EMAIL-08).
   - `buildBookingCancellationEmail(data)`: Cancellation notification email with ref code, schedule, and cancellation reason.
   - HTML escaping helper to prevent XSS in email content.
3. Create `src/lib/email/templates.test.ts` verifying:
   - HTML and plaintext structure for all 5 template types.
   - Correct Asia/Jakarta (WIB) date and time formatting.
   - Presence of CTA links with `APP_BASE_URL` resolution.
   - Proper escaping of special characters.
  </action>
  <verify>node --import tsx --test src/lib/email/templates.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/email/templates.ts` exports all template builder functions returning `{ subject, html, text }`.
    - HTML templates contain table containers with inline styling, status badges, and action buttons.
    - Plaintext versions are cleanly formatted without HTML tags.
    - All tests in `src/lib/email/templates.test.ts` pass with 0 failures.
  </acceptance_criteria>
  <done>Email template engine generates responsive HTML and plaintext outputs with WIB formatting and passes all unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Resend Email Service, Validator, Mock Provider & Audit Logger</name>
  <files>src/lib/email/service.server.ts, src/lib/email/service.test.ts</files>
  <read_first>src/lib/audit/audit.server.ts, src/lib/whatsapp/service.server.ts, .planning/phases/08-dual-channel-notification-integration/08-CONTEXT.md, .planning/phases/08-dual-channel-notification-integration/08-RESEARCH.md</read_first>
  <action>
1. Create `src/lib/email/service.server.ts` implementing:
   - `sanitizeEmail(email: string): string | null`: Trims, converts to lowercase, and validates against RFC 5322 regex pattern.
   - `sanitizeEmailList(emails: string | string[]): string[]`: Parses single string, comma-separated string (`"a@b.com, c@d.com"`), or string array, filtering invalid addresses.
   - `EmailService.sendEmail(params)`:
     - Sanitizes recipients; if empty or invalid, logs warning, logs failed audit event if `bookingId` present, and returns `{ success: false, error: "..." }` (EMAIL-03).
     - Checks mock mode (`!process.env.RESEND_API_KEY` || `process.env.RESEND_MOCK === "true"` || `process.env.NODE_ENV === "test"`): prints structured ASCII box to console, records `action: "notification.email_dispatch"` with `status: "mock"` to `audit_logs`, and returns mock success (EMAIL-02).
     - Dispatches real POST request to `https://api.resend.com/emails` with Bearer auth, `EMAIL_FROM`, `to`, `subject`, `html`, `text`, and timeout signal (EMAIL-01).
     - Records immutable audit log with `action: "notification.email_dispatch"`, `status: "success"` or `"failed"`, target, template type, and response metadata (NOTIF-02).
   - `safeDispatchEmail(params)`: Safe non-blocking wrapper that catches any unexpected runtime exceptions and returns `{ success: false, error: "..." }` without throwing (EMAIL-04).
2. Create `src/lib/email/service.test.ts` verifying:
   - Email sanitization for valid, invalid, and comma-separated addresses.
   - Mock fallback behavior when `RESEND_API_KEY` is missing or in test mode.
   - Audit log creation with `notification.email_dispatch`.
   - Real HTTP request formatting using mocked `fetch`.
   - Safe error handling in `safeDispatchEmail`.
  </action>
  <verify>node --import tsx --test src/lib/email/service.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/email/service.server.ts` exports `EmailService`, `sendEmail`, `safeDispatchEmail`, `sanitizeEmail`, and `sanitizeEmailList`.
    - Resend mock provider prints structured ASCII boxes and records audit logs without real network calls.
    - All tests in `src/lib/email/service.test.ts` pass with 0 failures.
  </acceptance_criteria>
  <done>Resend email service with validation, mock fallback, safe wrapper, and audit logging is fully implemented and tested.</done>
</task>

</tasks>
