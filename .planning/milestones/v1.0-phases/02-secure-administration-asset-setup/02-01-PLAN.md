---
phase: 02-secure-administration-asset-setup
plan: 01
type: execute
wave: 1
depends_on: ["01-01-PLAN.md"]
files_modified:
  - src/db/schema.ts
  - src/db/auth.server.ts
  - src/lib/auth-client.ts
  - src/routes/api/auth/$.ts
  - src/lib/auth.middleware.ts
  - src/db/auth.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
user_setup: []
must_haves:
  truths:
    - "D-01: Install and configure better-auth along with @better-auth/drizzle-adapter to manage session state in PostgreSQL database tables."
    - "D-02: Disable public user registration entirely to prevent unauthorized users from creating admin accounts."
    - "D-04: Better Auth configuration file must be stored in src/db/auth.server.ts to prevent leaks of server environment variables to isomorphic client bundles."
    - "D-05: Enforce authentication and authorization boundaries using TanStack Start middleware (createMiddleware) to intercept requests and validate user roles and sessions."
    - "D-06: Simple hierarchical role model: admin can perform all actions, operator can manage assets/schedules/bookings, pimpinan has read-only access."
    - "D-07: Throw standard HTTP 401 (Unauthorized) and 403 (Forbidden) errors at the server function boundary on access failure, allowing TanStack Router's error boundaries or redirects to catch them."
    - "D-08: Sourced user roles from the session object cookie-cached payload instead of re-querying the database on every request to optimize performance."
    - "D-14: Add a status column ('active', 'inactive') to the user table and verify it in the auth middleware."
  artifacts:
    - "src/db/schema.ts"
    - "src/db/auth.server.ts"
    - "src/lib/auth-client.ts"
    - "src/routes/api/auth/$.ts"
    - "src/lib/auth.middleware.ts"
    - "src/db/auth.test.ts"
  key_links:
    - "src/routes/api/auth/$.ts references src/db/auth.server.ts"
    - "src/lib/auth.middleware.ts references src/db/auth.server.ts"
---

<objective>
Establish the secure authentication and authorization foundation. Implement Better Auth Drizzle setup, database schemas extension, Catch-All auth API endpoint, client initialization, and server-side RBAC middleware.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-secure-administration-asset-setup/02-CONTEXT.md
@.planning/phases/02-secure-administration-asset-setup/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add availability schemas and user status to schema.ts</name>
  <files>src/db/schema.ts</files>
  <read_first>src/db/schema.ts</read_first>
  <action>Extend users table with status column ('active', 'inactive', default 'active') and add mustResetPassword boolean column (default false). Define asset_availability (id, assetId, dayOfWeek integer, openTime text, closeTime text) and asset_closures (id, assetId, date timestamp) tables and their Drizzle relations.</action>
  <verify>npx drizzle-kit generate</verify>
  <acceptance_criteria>
    - schema.ts contains definition for users status and mustResetPassword columns.
    - schema.ts contains definitions for assetAvailability and assetClosures tables.
  </acceptance_criteria>
  <done>src/db/schema.ts contains assetAvailability and assetClosures schemas</done>
</task>

<task type="auto">
  <name>Task 2: Configure Better Auth Server and Client SDK</name>
  <files>src/db/auth.server.ts, src/lib/auth-client.ts, src/routes/api/auth/$.ts</files>
  <read_first>src/db/schema.ts</read_first>
  <action>Create src/db/auth.server.ts initializing Better Auth with drizzleAdapter (using pg provider and schema), disabling public signUp, mapping roles, and adding the tanstackStartCookies plugin. Create src/lib/auth-client.ts initializing createAuthClient. Create src/routes/api/auth/$.ts catching all auth endpoints and mounting GET/POST handlers.</action>
  <verify>npx tsx -e "import { auth } from './src/db/auth.server'; console.log('Auth config loaded:', !!auth)"</verify>
  <acceptance_criteria>
    - src/db/auth.server.ts has signUp: { enabled: false } and plugins: [tanstackStartCookies()].
    - src/routes/api/auth/$.ts exports route handlers for GET and POST using auth.handler.
  </acceptance_criteria>
  <done>src/db/auth.server.ts and src/routes/api/auth/$.ts are created</done>
</task>

<task type="auto">
  <name>Task 3: Implement Auth Middleware for TanStack Start</name>
  <files>src/lib/auth.middleware.ts</files>
  <read_first>src/db/auth.server.ts</read_first>
  <action>Create src/lib/auth.middleware.ts implementing authMiddleware via createMiddleware. Intercept requests, get request headers, fetch session using auth.api.getSession, verify active session, check user status is 'active', and append user and session context. Implement requireRole helper checking permissions: admin (all), operator (assets, schedule, bookings), pimpinan (read-only).</action>
  <verify>npx tsx -e "import { authMiddleware } from './src/lib/auth.middleware'; console.log('Auth middleware exists:', !!authMiddleware)"</verify>
  <acceptance_criteria>
    - authMiddleware checks user status and throws unauthorized error.
    - Helper throws forbidden error if role hierarchy is violated.
  </acceptance_criteria>
  <done>src/lib/auth.middleware.ts exists</done>
</task>

<task type="auto">
  <name>Task 4: Write tests and run migrations</name>
  <files>src/db/auth.test.ts, package.json</files>
  <read_first>src/db/schema.ts, src/db/auth.server.ts, src/lib/auth.middleware.ts</read_first>
  <action>Create database migrations using drizzle-kit generate and run them using npm run db:migrate. Create src/db/auth.test.ts to test: 1) schema definitions for availability/closures, 2) Better Auth options (signUp disabled, tanstack plugin), 3) authMiddleware permissions and roles hierarchy. Add test script to package.json pointing to built-in node runner.</action>
  <verify>node --import tsx --test src/db/auth.test.ts</verify>
  <acceptance_criteria>
    - Database migrations are generated in drizzle folder and executed.
    - auth.test.ts tests the middleware throwing errors on unauthenticated/unauthorized inputs.
    - Tests pass.
  </acceptance_criteria>
  <done>node --import tsx --test src/db/auth.test.ts runs successfully</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --import tsx --test src/db/auth.test.ts runs and passes
- [ ] npm run generate-routes runs and updates route tree
</verification>

<success_criteria>
- Extend database schema and run migrations successfully.
- Better Auth setup prevents public registration.
- Catch-All route API accepts auth requests.
- Auth middleware protects server-boundary functions and enforces hierarchy permissions.
- Tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-secure-administration-asset-setup/02-01-SUMMARY.md`
</output>
