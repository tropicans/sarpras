---
phase: 02-secure-administration-asset-setup
plan: 03
type: execute
wave: 3
depends_on: ["02-02-PLAN.md"]
files_modified:
  - src/routes/admin/assets.tsx
  - src/db/auth.test.ts
autonomous: true
requirements:
  - ASSET-01
  - ASSET-02
  - ASSET-03
user_setup: []
must_haves:
  truths:
    - "D-09: Model weekly operating availability schedules in a separate relational table (asset_availability referencing the assets.id column)."
    - "D-10: Model date-specific closures in a separate relational table (asset_closures referencing the assets.id column)."
    - "D-11: Interpret all operating hours and closures in Asia/Jakarta local time zone, doing timezone-aware boundary checks on the server."
    - "D-12: Asset soft deletion using a status column ('active', 'inactive', 'archived') in the assets table, blocking bookings if archived, and retaining all historical booking and audit records."
  artifacts:
    - "src/routes/admin/assets.tsx"
  key_links:
    - "src/routes/admin/assets.tsx references src/db/schema.ts"
---

<objective>
Implement the Asset management interface and availability configuration. Build the Asset CRUD panel, soft delete / archiving controls, weekly operating hours configuration, date-specific closure schedulers, and server-side timezone-aware validation.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-secure-administration-asset-setup/02-CONTEXT.md
@.planning/phases/02-secure-administration-asset-setup/02-UI-SPEC.md
@.planning/phases/02-secure-administration-asset-setup/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Asset CRUD and soft deletion</name>
  <files>src/routes/admin/assets.tsx</files>
  <read_first>src/db/schema.ts, src/lib/auth.middleware.ts</read_first>
  <action>Create src/routes/admin/assets.tsx layout and listing. Implement listing table with columns for Name, Type, Location, Capacity, and Status. Write protected server functions to query, insert, and update assets. Implement soft deletion / archiving: a server function to update an asset's status to 'archived' when archived (using the dialog from UI-SPEC).</action>
  <verify>npm run generate-routes</verify>
  <acceptance_criteria>
    - assets.tsx lists assets with badged indicators for 'Room' or 'Dormitory'.
    - Empty state displays 'No assets found' and description from UI-SPEC.
    - Archiving updates asset status to 'archived' without deleting relational rows.
  </acceptance_criteria>
  <done>Asset CRUD listing and soft deletion API functions exist</done>
</task>

<task type="auto">
  <name>Task 2: Build Operating Availability and Closures scheduling</name>
  <files>src/routes/admin/assets.tsx</files>
  <read_first>src/db/schema.ts, src/routes/admin/assets.tsx</read_first>
  <action>Extend src/routes/admin/assets.tsx with schedule configuration panel. Render form to configure weekly operating availability (dayOfWeek, openTime, closeTime) and a list to add closure dates. Write protected server functions to save weekly availability into asset_availability table and closure dates into asset_closures table.</action>
  <verify>npm run generate-routes</verify>
  <acceptance_criteria>
    - Scheduling forms allow selecting days of the week and entering start/end times.
    - Forms allow selecting specific closure dates and adding them to the list.
    - Data is written to asset_availability and asset_closures tables.
  </acceptance_criteria>
  <done>Asset availability scheduling UI and APIs are complete</done>
</task>

<task type="auto">
  <name>Task 3: Implement timezone-aware server-side validations</name>
  <files>src/routes/admin/assets.tsx</files>
  <read_first>src/routes/admin/assets.tsx</read_first>
  <action>Write server-side validation functions verifying: 1) End time is strictly after start time in weekly schedule, 2) Closure dates are valid, 3) All inputs are interpreted in Asia/Jakarta timezone using date-fns-tz helper libraries before executing DB mutations.</action>
  <verify>npm run check</verify>
  <acceptance_criteria>
    - Server functions reject invalid start/end schedules with validation errors.
    - Date parsing interprets raw date strings as Asia/Jakarta local times.
  </acceptance_criteria>
  <done>Server-side availability schedule timezone checks are implemented</done>
</task>

<task type="auto">
  <name>Task 4: Write tests for asset scheduling and timezone validation</name>
  <files>src/db/auth.test.ts</files>
  <read_first>src/db/auth.test.ts, src/routes/admin/assets.tsx</read_first>
  <action>Add tests to src/db/auth.test.ts verifying: 1) Asset archiving successfully changes status to 'archived' in database, 2) Asset availability validation correctly accepts valid slot times and rejects end time before start time, 3) Schedule validations interpret timestamps in Asia/Jakarta correctly (e.g. 08:00 WIB translates to UTC+7 offset check).</action>
  <verify>node --import tsx --test src/db/auth.test.ts</verify>
  <acceptance_criteria>
    - Tests verify timezone-aware validation logic.
    - Tests pass.
  </acceptance_criteria>
  <done>node --import tsx --test src/db/auth.test.ts runs successfully</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --import tsx --test src/db/auth.test.ts runs and passes
- [ ] npm run generate-routes runs and updates route tree
</verification>

<success_criteria>
- Asset CRUD interface manages asset records successfully.
- Archiving is soft-deletion, preserving historical data.
- Asset weekly operating hours and closure dates are saved in respective relational tables.
- Scheduling validations perform timezone-aware checks strictly under Asia/Jakarta.
- Tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-secure-administration-asset-setup/02-03-SUMMARY.md`
</output>
