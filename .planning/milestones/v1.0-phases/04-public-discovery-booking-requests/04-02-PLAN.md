---
phase: 04-public-discovery-booking-requests
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/public/public-header.tsx
  - src/components/public/public-footer.tsx
  - src/components/public/asset-card.tsx
  - src/components/public/schedule-modal.tsx
  - src/routes/index.tsx
autonomous: true
requirements:
  - ASSET-04
user_setup: []
must_haves:
  truths:
    - "D-01: Asset catalog displays rooms and dormitories as interactive cards with type filters (Semua, Ruang Rapat, Asrama/Wisma), capacity info, and live availability badges."
    - "D-02: Modal/Drawer calendar view ('Lihat Jadwal') displays scheduled time blocks and closures strictly without requester names, organizations, or purpose details."
    - "D-08: Home page features Hero banner with quick availability search, asset catalog grid, 'Cara Pengajuan' 3-step guide, and public navigation header."
  artifacts:
    - "src/components/public/public-header.tsx"
    - "src/components/public/public-footer.tsx"
    - "src/components/public/asset-card.tsx"
    - "src/components/public/schedule-modal.tsx"
    - "src/routes/index.tsx"
  key_links:
    - "src/routes/index.tsx imports components from src/components/public/"
    - "src/routes/index.tsx loads assets using getPublicAssetsListFn"
---

<objective>
Build the public portal home page (`/`) featuring a modern Hero section, 'Cara Pengajuan' 3-step guide, interactive Asset Catalog with type/date filters, and privacy-safe 'Lihat Jadwal' schedule modal.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-public-discovery-booking-requests/04-CONTEXT.md
@.planning/phases/04-public-discovery-booking-requests/04-RESEARCH.md
@.planning/phases/04-public-discovery-booking-requests/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Public Header and Footer Components</name>
  <files>src/components/public/public-header.tsx, src/components/public/public-footer.tsx</files>
  <read_first>src/routes/admin.tsx, src/styles.css</read_first>
  <action>Create public layout components:
1. `src/components/public/public-header.tsx`:
   - Government institution branding ("Sarpras PPKASN" / "Pusat Pengembangan Kompetensi ASN").
   - Navigation links: "Beranda" (`/`), "Katalog Sarana" (`/#katalog`), "Cek Status" (`/status`), and "Masuk Petugas" (`/login`).
   - Clean, sticky header with subtle backdrop blur and mobile responsive menu toggle.
2. `src/components/public/public-footer.tsx`:
   - Institutional footer with contact information, office address, operating schedule summary, and system version information.</action>
  <verify>& "C:\nvm4w\nodejs\node.exe" -e "import('./src/components/public/public-header.tsx').then(() => console.log('Header OK'))"</verify>
  <acceptance_criteria>
    - `public-header.tsx` and `public-footer.tsx` render responsive UI using Tailwind classes and Lucide icons.
    - Navigation links point to valid application routes (`/`, `/status`, `/login`).
  </acceptance_criteria>
  <done>Public header and footer components created</done>
</task>

<task type="auto">
  <name>Task 2: Build Asset Card and Schedule Modal Components</name>
  <files>src/components/public/asset-card.tsx, src/components/public/schedule-modal.tsx</files>
  <read_first>src/lib/booking/public-fns.server.ts, src/components/ui/button.tsx</read_first>
  <action>Create asset discovery components:
1. `src/components/public/asset-card.tsx`:
   - Displays asset name, type badge ("Ruang Rapat" / "Asrama & Wisma"), location/floor, capacity pill (`Kapasitas: X Orang`), and status badge ("Tersedia", "Terisi Sebagian", "Penuh / Tutup").
   - Action buttons: "Lihat Jadwal" (opens schedule modal) and "Ajukan Pinjam" (navigates to `/book/$assetId`).
2. `src/components/public/schedule-modal.tsx`:
   - Modal/dialog that fetches public schedule using `getAssetPublicScheduleFn({ data: assetId })`.
   - Renders a clean visual day/week schedule or monthly calendar showing booked slots ("Terpakai") and closure blocks ("Tutup / Pemeliharaan").
   - STRICT PRIVACY GUARANTEE: absolutely zero requester names, phone numbers, or purpose descriptions rendered (D-02, ASSET-04).</action>
  <verify>& "C:\nvm4w\nodejs\node.exe" -e "import('./src/components/public/asset-card.tsx').then(() => console.log('Asset card OK'))"</verify>
  <acceptance_criteria>
    - `asset-card.tsx` displays metadata and triggers schedule modal / booking route.
    - `schedule-modal.tsx` renders booked time slots anonymously without any personal details.
  </acceptance_criteria>
  <done>Asset card and privacy-safe schedule modal components created</done>
</task>

<task type="auto">
  <name>Task 3: Implement Public Home Page Route</name>
  <files>src/routes/index.tsx</files>
  <read_first>src/routes/index.tsx, src/components/public/public-header.tsx, src/components/public/asset-card.tsx</read_first>
  <action>Rewrite `src/routes/index.tsx`:
1. Use `createFileRoute('/')` with a route loader fetching active assets using `getPublicAssetsListFn`.
2. Section 1: Hero Banner:
   - Catchy title: "Layanan Peminjaman Sarana & Prasarana PPKASN".
   - Subtitle explaining easy, transparent, and accountable room and dormitory booking.
   - Quick Filter / Search Bar (search keyword, asset type filter pills: "Semua", "Ruang Rapat", "Asrama/Wisma").
   - Primary CTA: "Cari Sarana" (smooth scrolls to catalog) and "Cek Status Permohonan" (links to `/status`).
3. Section 2: "Cara Pengajuan" 3-Step Guide (D-08):
   - Step 1: "Pilih Sarana & Cek Jadwal" (Pilih ruangan atau kamar dan periksa ketersediaan tanggal).
   - Step 2: "Isi Formulir Permohonan" (Lengkapi identitas pemohon dan rincian kegiatan).
   - Step 3: "Dapatkan Kode Referensi & Pantau" (Simpan kode unik dan pantau proses persetujuan oleh petugas).
4. Section 3: Asset Catalog Grid (`#katalog`):
   - Live search input & category filter buttons.
   - Responsive grid rendering `AssetCard` components.
   - Empty state when no assets match the search query.
5. Integrated `PublicHeader` and `PublicFooter`.</action>
  <verify>& "C:\nvm4w\nodejs\node.exe" -e "import('./src/routes/index.tsx').then(() => console.log('Home route OK'))"</verify>
  <acceptance_criteria>
    - `src/routes/index.tsx` loads active assets via TanStack route loader.
    - Search and type filtering works seamlessly in the UI.
    - "Lihat Jadwal" opens privacy-safe schedule modal for each asset.
  </acceptance_criteria>
  <done>Public portal home page is fully functional and responsive</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `& "C:\nvm4w\nodejs\node.exe" --import tsx --test src/lib/booking/booking.test.ts` runs and passes.
- [ ] Route compilation and TypeScript checks pass.
</verification>

<success_criteria>
- Public home page presents modern hero banner, 3-step guide, and interactive asset catalog.
- Type and search filters allow instant asset discovery.
- Privacy-safe schedule modal shows real-time booking slots with zero requester PII.
</success_criteria>

<output>
After completion, create `.planning/phases/04-public-discovery-booking-requests/04-02-SUMMARY.md`
</output>
