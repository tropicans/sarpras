---
phase: 04-public-discovery-booking-requests
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/components/booking/wizard-stepper.tsx
  - src/components/booking/schedule-step.tsx
  - src/components/booking/requester-step.tsx
  - src/components/booking/review-step.tsx
  - src/components/booking/success-card.tsx
  - src/routes/book/$assetId.tsx
  - src/routes/status/index.tsx
  - src/routes/status/$ref.tsx
  - src/routeTree.gen.ts
autonomous: true
requirements:
  - BOOK-01
  - BOOK-02
  - BOOK-03
  - BOOK-04
  - BOOK-05
user_setup: []
must_haves:
  truths:
    - "D-03: Guided 3-step wizard on `/book/$assetId`: Step 1 (Schedule & Attendees with pre-flight check), Step 2 (Requester Info), Step 3 (Review & Submit)."
    - "D-04: Step 1 performs instant availability pre-flight checks (operating hours, closures, room overlap, dorm capacity) with visual feedback."
    - "D-05: Returns non-guessable reference code on submission with copy-to-clipboard button and direct link to status tracking."
    - "D-06: `/status` and `/status/$ref` display progress timeline (Menunggu Konfirmasi -> Disetujui / Ditolak [with reason] / Dibatalkan), asset summary, and schedule."
    - "D-07: Public self-service cancellation allows requesters to cancel their pending or approved booking directly from the tracking page."
  artifacts:
    - "src/components/booking/wizard-stepper.tsx"
    - "src/components/booking/schedule-step.tsx"
    - "src/components/booking/requester-step.tsx"
    - "src/components/booking/review-step.tsx"
    - "src/components/booking/success-card.tsx"
    - "src/routes/book/$assetId.tsx"
    - "src/routes/status/index.tsx"
    - "src/routes/status/$ref.tsx"
  key_links:
    - "src/routes/book/$assetId.tsx uses submitBookingRequestFn"
    - "src/routes/status/$ref.tsx uses getPublicBookingStatusFn and cancelBookingByPublicReferenceFn"
---

<objective>
Implement the 3-step public booking request wizard (`/book/$assetId`) and the public request status tracking & self-service cancellation pages (`/status` and `/status/$ref`).
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-public-discovery-booking-requests/04-CONTEXT.md
@.planning/phases/04-public-discovery-booking-requests/04-RESEARCH.md
@.planning/phases/04-public-discovery-booking-requests/04-01-PLAN.md
@.planning/phases/04-public-discovery-booking-requests/04-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Booking Wizard Step Components</name>
  <files>
    src/components/booking/wizard-stepper.tsx,
    src/components/booking/schedule-step.tsx,
    src/components/booking/requester-step.tsx,
    src/components/booking/review-step.tsx,
    src/components/booking/success-card.tsx
  </files>
  <read_first>src/lib/booking/types.ts, src/lib/booking/public-fns.server.ts</read_first>
  <action>Create modular step components for the wizard:
1. `src/components/booking/wizard-stepper.tsx`:
   - Visual progress indicator showing Step 1: "Jadwal & Peserta", Step 2: "Data Pemohon", Step 3: "Konfirmasi & Kirim".
2. `src/components/booking/schedule-step.tsx`:
   - For rooms: date picker, start time (HH:mm), end time (HH:mm), and attendance number input.
   - For dormitories: check-in date picker, check-out date picker, and guest count input.
   - Triggers `checkAvailabilityPreflightFn` on value change to display live badges: green checkmark if slot is free, red error with clear Indonesian message if outside hours / closed / over capacity / overlapping.
3. `src/components/booking/requester-step.tsx`:
   - Inputs for: Nama Lengkap Pemohon, Email Pemohon (valid email format), Nomor WhatsApp / Telepon, Unit Kerja / Instansi, and Tujuan Penggunaan / Nama Kegiatan.
   - Field-level validation messages.
4. `src/components/booking/review-step.tsx`:
   - Clear summary card displaying all chosen parameters (Asset, Schedule in WIB, Attendee count, Requester info).
   - Terms & condition checkbox ("Saya menyatakan data yang diisikan benar dan mematuhi tata tertib penggunaan sarana").
5. `src/components/booking/success-card.tsx`:
   - Success checkmark icon.
   - Displays Reference Code (e.g. `SP-2026-XXXXX` or booking ID) with "Salin Kode" (copy-to-clipboard) button.
   - Action buttons: "Pantau Status Permohonan" (navigates to `/status/$ref`) and "Kembali ke Beranda" (`/`).</action>
  <verify>& "C:\nvm4w\nodejs\node.exe" -e "import('./src/components/booking/wizard-stepper.tsx').then(() => console.log('Wizard components OK'))"</verify>
  <acceptance_criteria>
    - All wizard components are created and typed with TypeScript.
    - Preflight validation in `schedule-step.tsx` provides instant feedback.
    - `success-card.tsx` provides copy-to-clipboard and navigation to tracking.
  </acceptance_criteria>
  <done>Booking wizard step components created</done>
</task>

<task type="auto">
  <name>Task 2: Implement Booking Wizard Route (/book/$assetId)</name>
  <files>src/routes/book/$assetId.tsx</files>
  <read_first>src/routes/index.tsx, src/lib/booking/server-fns.server.ts, src/components/booking/wizard-stepper.tsx</read_first>
  <action>Create `src/routes/book/$assetId.tsx`:
1. Use `createFileRoute('/book/$assetId')` with a loader that fetches asset details by `assetId` (throws 404 if asset not found or inactive).
2. Manages wizard active step state (1, 2, 3, or 'success').
3. Handles draft state across steps so going back doesn't lose previously entered requester information.
4. On Step 3 submission:
   - Calls `submitBookingRequestFn({ data: bookingPayload })`.
   - On success: switches view to `SuccessCard` with returned booking reference.
   - On conflict / error: displays alert banner with specific conflict reason.</action>
  <verify>& "C:\nvm4w\nodejs\node.exe" -e "import('./src/routes/book/$assetId.tsx').then(() => console.log('Book route OK'))"</verify>
  <acceptance_criteria>
    - `/book/$assetId` renders the asset header, stepper, and active step form.
    - Submitting step 3 creates a booking via `submitBookingRequestFn` and transitions to success view.
  </acceptance_criteria>
  <done>Dedicated booking wizard route implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement Public Status Tracking & Cancellation Routes (/status & /status/$ref)</name>
  <files>src/routes/status/index.tsx, src/routes/status/$ref.tsx</files>
  <read_first>src/lib/booking/public-fns.server.ts, src/lib/booking/server-fns.server.ts</read_first>
  <action>Implement the status tracking routes:
1. `src/routes/status/index.tsx`:
   - Search form prompting visitor for "Kode Referensi atau ID Permohonan".
   - Submitting navigates to `/status/$ref` (or `/status?ref=...`).
   - Helpful tips explaining where to find the reference code (from submission screen).
2. `src/routes/status/$ref.tsx`:
   - Route loader fetches public booking status via `getPublicBookingStatusFn({ data: { ref } })`.
   - Renders visual status timeline:
     - `pending`: "Menunggu Konfirmasi Petugas" (Yellow badge / hourglass icon).
     - `approved`: "Permohonan Disetujui" (Green badge / check icon).
     - `rejected`: "Permohonan Ditolak" (Red badge / X icon + Box showing official rejection reason).
     - `cancelled`: "Permohonan Dibatalkan" (Gray badge / cancel icon).
   - Displays Asset summary (Name, Location, Type) and Schedule details in WIB (`Asia/Jakarta`).
   - Public Self-Service Cancellation (D-07):
     - If status is `pending` or `approved`: show "Batalkan Permohonan" button.
     - Opens confirmation modal asking for confirmation and optional cancellation reason.
     - Submitting calls `cancelBookingByPublicReferenceFn({ data: { bookingId: booking.id, referenceToken: ref, reason } })`.
     - Refreshes router / loader data to show updated `cancelled` status.</action>
  <verify>& "C:\nvm4w\nodejs\node.exe" -e "import('./src/routes/status/$ref.tsx').then(() => console.log('Status route OK'))"</verify>
  <acceptance_criteria>
    - `/status` provides a search bar to look up requests.
    - `/status/$ref` renders the status timeline and rejection reason if rejected.
    - Self-service cancellation updates the booking to `cancelled` with audit trail.
  </acceptance_criteria>
  <done>Public status tracking and cancellation routes implemented</done>
</task>

<task type="auto">
  <name>Task 4: Regenerate Route Tree and Run Full Test Suite</name>
  <files>src/routeTree.gen.ts</files>
  <read_first>package.json</read_first>
  <action>Run TanStack Router route generator and execute the full test suite to guarantee zero regressions and complete type safety:
1. Run `& "C:\nvm4w\nodejs\node.exe" ./node_modules/@tanstack/router-cli/bin/tsr.cjs generate` or npm run generate-routes.
2. Run full test suite: `& "C:\nvm4w\nodejs\node.exe" --import tsx --test src/db/migration.test.ts src/db/auth.test.ts src/lib/booking/booking.test.ts`.</action>
  <verify>& "C:\nvm4w\nodejs\node.exe" --import tsx --test src/db/migration.test.ts src/db/auth.test.ts src/lib/booking/booking.test.ts</verify>
  <acceptance_criteria>
    - `src/routeTree.gen.ts` contains all new public routes (`/`, `/book/$assetId`, `/status`, `/status/$ref`).
    - All 23+ automated tests pass without errors.
  </acceptance_criteria>
  <done>Route tree generated and full test suite passes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `& "C:\nvm4w\nodejs\node.exe" --import tsx --test src/db/migration.test.ts src/db/auth.test.ts src/lib/booking/booking.test.ts` passes.
- [ ] Route tree is up to date and all public routes resolve cleanly.
</verification>

<success_criteria>
- 3-step booking wizard provides instant pre-flight availability feedback and submits structured requests.
- Visitors receive a copyable reference code and can track their request status on a visual timeline.
- Requesters can self-cancel their pending or approved booking using their reference token.
</success_criteria>

<output>
After completion, create `.planning/phases/04-public-discovery-booking-requests/04-03-SUMMARY.md`
</output>
