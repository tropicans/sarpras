---
phase: 05-administrative-decisions-operations
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/routes/admin.tsx
  - src/routes/admin/index.tsx
  - src/routes/admin/bookings.tsx
  - src/components/admin/kpi-card.tsx
  - src/components/admin/urgent-bookings-widget.tsx
  - src/components/admin/bookings-filter-bar.tsx
  - src/components/admin/booking-review-drawer.tsx
  - src/components/admin/rejection-reason-modal.tsx
autonomous: true
requirements:
  - FLOW-02
  - FLOW-03
  - OPS-01
user_setup: []
must_haves:
  truths:
    - "D-01 & FLOW-02: Bookings management table renders all requests with status badges, filters, and a slide-out Review Drawer displaying full requester PII, schedule, and live conflict warnings."
    - "D-02 & FLOW-03: Rejection modal allows administrators to select common preset reasons or input custom justifications, recording the explanation permanently."
    - "D-03 & OPS-01: Admin dashboard features 4 summary KPI metric cards (Pending Action, Approved Month, Active Assets, Active Closures) and an Urgent Action queue widget for swift operational response."
    - "D-04 & OPS-01: Filter toolbar provides reactive filtering by status, asset type, date presets/ranges, and search by reference ID, name, or organization."
  artifacts:
    - "src/routes/admin.tsx"
    - "src/routes/admin/index.tsx"
    - "src/routes/admin/bookings.tsx"
    - "src/components/admin/kpi-card.tsx"
    - "src/components/admin/urgent-bookings-widget.tsx"
    - "src/components/admin/bookings-filter-bar.tsx"
    - "src/components/admin/booking-review-drawer.tsx"
    - "src/components/admin/rejection-reason-modal.tsx"
  key_links:
    - "src/routes/admin.tsx links to /admin/bookings, /admin/calendar, and /admin/audit"
    - "src/routes/admin/bookings.tsx uses BookingReviewDrawer and RejectionReasonModal"
    - "src/routes/admin/index.tsx uses KpiCard and UrgentBookingsWidget"
---

<objective>
Implement the administrative dashboard overview and the interactive bookings management queue with filter toolbar, slide-out review drawer, live conflict detection display, and structured rejection reason modal.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-administrative-decisions-operations/05-CONTEXT.md
@.planning/phases/05-administrative-decisions-operations/05-RESEARCH.md
@.planning/phases/05-administrative-decisions-operations/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Admin Layout Sidebar Navigation & Dashboard Overview</name>
  <files>src/routes/admin.tsx, src/routes/admin/index.tsx, src/components/admin/kpi-card.tsx, src/components/admin/urgent-bookings-widget.tsx</files>
  <read_first>src/routes/admin.tsx, src/routes/admin/index.tsx, src/lib/booking/admin-fns.server.ts</read_first>
  <action>1. Update `src/routes/admin.tsx` sidebar navigation:
   - Add navigation links for:
     - Dashboard (`/admin`) with `LayoutDashboard` icon.
     - Permohonan Booking (`/admin/bookings`) with `CalendarCheck2` or `Inbox` icon and a badge count for pending requests if available.
     - Kalender Operasional (`/admin/calendar`) with `Calendar` icon.
     - Manajemen Aset (`/admin/assets`) with `Building2` icon.
     - Manajemen Pengguna (`/admin/users`) with `Users` icon.
     - Riwayat Audit (`/admin/audit`) with `ScrollText` or `History` icon.
2. Create `src/components/admin/kpi-card.tsx`:
   - Visual card with title, large numeric value, icon, subtext (e.g. "Butuh tindakan segera"), and accent color styling.
3. Create `src/components/admin/urgent-bookings-widget.tsx`:
   - Prioritized list showing pending requests ordered by creation date.
   - Shows requester name, asset name, requested dates, and quick "Review" button that opens the booking review drawer or navigates to `/admin/bookings?status=pending`.
4. Update `src/routes/admin/index.tsx`:
   - Load dashboard data using `getAdminDashboardOverviewFn()`.
   - Render 4 KPI cards:
     1. "Perlu Tindakan" (Pending bookings count, highlighted amber/red if > 0).
     2. "Booking Disetujui (Bulan Ini)" (Approved bookings in current calendar month).
     3. "Aset Aktif" (Total active assets).
     4. "Penutupan Aktif" (Active closures count).
   - Render `UrgentBookingsWidget` alongside quick administrative action links.</action>
  <verify>& "C:\nvm4w\nodejs\node.exe" -e "import('./src/routes/admin.tsx'); import('./src/routes/admin/index.tsx'); console.log('Admin navigation & dashboard verified')"</verify>
  <acceptance_criteria>
    - Sidebar has all 6 admin routes: Dashboard, Bookings, Calendar, Assets, Users, Audit.
    - Dashboard displays 4 KPI cards and urgent pending bookings widget.
  </acceptance_criteria>
  <done>Sidebar navigation and dashboard KPI overview implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement Filter Bar, Review Drawer, and Rejection Modal Components</name>
  <files>src/components/admin/bookings-filter-bar.tsx, src/components/admin/booking-review-drawer.tsx, src/components/admin/rejection-reason-modal.tsx</files>
  <read_first>src/lib/booking/admin-fns.server.ts, src/lib/booking/service.server.ts, src/lib/timezone/datetime.ts</read_first>
  <action>1. Create `src/components/admin/bookings-filter-bar.tsx`:
   - Status selector tabs/dropdown (`Semua`, `Pending`, `Disetujui`, `Ditolak`, `Dibatalkan`).
   - Asset type filter (`Semua Tipe`, `Ruang Rapat`, `Asrama`).
   - Quick date preset filter (`Semua Waktu`, `Hari Ini`, `Minggu Ini`, `Bulan Ini`, `Kustom`).
   - Search input field (filtering reference ID, requester name, or organization).
2. Create `src/components/admin/booking-review-drawer.tsx`:
   - Slide-out drawer displaying comprehensive details of a selected booking:
     - Header with reference code, status badge, and submission timestamp.
     - Section 1: Detail Pemohon (Name, Email, Phone, Organization, Purpose).
     - Section 2: Jadwal & Fasilitas (Asset Name, Type, Location, Start/End dates formatted in WIB, Attendance / Headcount).
     - Section 3: Live Conflict Inspector (`getBookingConflictContextFn`):
       - If `hasHardConflict`: Bold red warning listing overlapping approved bookings with dates and requester names.
       - If `hasPendingOverlaps`: Amber warning indicating competing pending requests for the same slot.
       - If clean: Green badge indicating "Tidak ada konflik jadwal".
     - Action footer: Quick "Setujui Permohonan" button (disabled or requiring confirmation if hard conflict) and "Tolak Permohonan" button.
3. Create `src/components/admin/rejection-reason-modal.tsx`:
   - Modal triggered when admin clicks "Tolak Permohonan".
   - Preset justification select dropdown:
     - "Jadwal Bertabrakan dengan Kegiatan Lain"
     - "Pemeliharaan / Penutupan Fasilitas"
     - "Kapasitas Fasilitas Tidak Memadai"
     - "Tidak Memenuhi Ketentuan Peminjaman"
     - "Lainnya (Tuliskan secara spesifik)"
   - Pre-fills a mandatory textarea where the admin can customize or add specifics.
   - Submits through `rejectBookingAdminFn` with validation error handling.</action>
  <verify>& "C:\nvm4w\nodejs\node.exe" -e "import('./src/components/admin/bookings-filter-bar.tsx'); import('./src/components/admin/booking-review-drawer.tsx'); import('./src/components/admin/rejection-reason-modal.tsx'); console.log('Components verified')"</verify>
  <acceptance_criteria>
    - Review drawer displays full requester PII, asset schedule, and live conflict indicators.
    - Rejection modal provides presets and enforces custom justification text.
    - Filter bar provides reactive controls.
  </acceptance_criteria>
  <done>Review drawer, rejection modal, and filter bar components created</done>
</task>

<task type="auto">
  <name>Task 3: Implement Bookings Management Route (/admin/bookings)</name>
  <files>src/routes/admin/bookings.tsx</files>
  <read_first>src/routes/admin.tsx, src/lib/booking/admin-fns.server.ts, src/components/admin/bookings-filter-bar.tsx, src/components/admin/booking-review-drawer.tsx</read_first>
  <action>Create `src/routes/admin/bookings.tsx` as a createFileRoute for `/admin/bookings`:
1. Search params validation with Zod: `status`, `assetType`, `startDate`, `endDate`, `search`, `page`.
2. Server loader / client fetching calling `getAdminBookingsFn(searchParams)`.
3. Render interactive table:
   - Columns: Ref / ID, Aset, Pemohon & Instansi, Jadwal (WIB), Jumlah Peserta, Status, Aksi.
   - Status badge styling (Pending = Amber, Disetujui = Emerald, Ditolak = Rose, Dibatalkan = Zinc).
   - Quick action buttons: "Tinjau / Review", quick "Setujui", quick "Tolak".
4. Integrated drawers & modals:
   - Clicking "Tinjau" opens `BookingReviewDrawer` for that booking.
   - Clicking "Setujui" triggers confirmation dialog and executes `approveBookingAdminFn`, refreshing the table.
   - Clicking "Tolak" opens `RejectionReasonModal` and executes `rejectBookingAdminFn`, refreshing the table.
5. Pagination bar with Next/Prev and page indicators.</action>
  <verify>& "C:\nvm4w\nodejs\node.exe" -e "import('./src/routes/admin/bookings.tsx').then(() => console.log('Admin bookings route verified'))"</verify>
  <acceptance_criteria>
    - `/admin/bookings` loads, filters, and paginates booking requests.
    - Approving updates status to `approved` and refreshes view.
    - Rejecting with reason updates status to `rejected` with saved justification.
  </acceptance_criteria>
  <done>src/routes/admin/bookings.tsx created and integrated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `& "C:\nvm4w\nodejs\node.exe" --import tsx --test src/lib/booking/admin.test.ts` passes.
- [ ] Manual check of `/admin` dashboard and `/admin/bookings` route structure.
</verification>

<success_criteria>
- Administrators have complete visibility over incoming bookings with reactive filtering and pagination.
- Review drawer surfaces all requester details and live conflict context.
- Approvals and rejections are accountable, transactional, and audit-logged.
</success_criteria>

<output>
After completion, create `.planning/phases/05-administrative-decisions-operations/05-02-SUMMARY.md`
</output>
