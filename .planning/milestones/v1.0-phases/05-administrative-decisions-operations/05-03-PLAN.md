---
phase: 05-administrative-decisions-operations
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/routes/admin/calendar.tsx
  - src/routes/admin/audit.tsx
  - src/components/admin/admin-calendar-view.tsx
  - src/components/admin/calendar-event-popover.tsx
  - src/components/admin/audit-table.tsx
  - src/components/admin/audit-diff-viewer.tsx
autonomous: true
requirements:
  - OPS-02
  - OPS-04
user_setup: []
must_haves:
  truths:
    - "D-05 & OPS-02: Operations calendar renders Month and Week views for selected asset(s), with distinct visual styling for Approved bookings (solid teal/emerald), Pending requests (amber striped), and Asset Closures (rose banner)."
    - "D-06 & OPS-02: Clicking any calendar event or slot displays a contextual preview popover with requester info, exact hours, and quick actions."
    - "D-07 & OPS-04: Audit history route (/admin/audit) provides a filterable, paginated trail of all system events with action, entity, actor, and date filters."
    - "D-08 & OPS-04: Audit events feature structured visual diffs for state transitions (e.g. status pending -> approved) and an expandable JSON view for raw metadata inspection."
  artifacts:
    - "src/routes/admin/calendar.tsx"
    - "src/routes/admin/audit.tsx"
    - "src/components/admin/admin-calendar-view.tsx"
    - "src/components/admin/calendar-event-popover.tsx"
    - "src/components/admin/audit-table.tsx"
    - "src/components/admin/audit-diff-viewer.tsx"
  key_links:
    - "src/routes/admin/calendar.tsx uses AdminCalendarView and CalendarEventPopover"
    - "src/routes/admin/audit.tsx uses AuditTable and AuditDiffViewer"
    - "src/components/admin/admin-calendar-view.tsx fetches from getAdminCalendarEventsFn"
    - "src/components/admin/audit-table.tsx fetches from getAdminAuditLogsFn"
---

<objective>
Implement the asset-centric operations calendar for weekly/monthly room and dormitory schedule management, and the system-wide audit history explorer with visual state diffs.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-administrative-decisions-operations/05-CONTEXT.md
@.planning/phases/05-administrative-decisions-operations/05-RESEARCH.md
@.planning/phases/05-administrative-decisions-operations/05-01-SUMMARY.md
@.planning/phases/05-administrative-decisions-operations/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Asset-Centric Operations Calendar & Event Popover</name>
  <files>src/components/admin/admin-calendar-view.tsx, src/components/admin/calendar-event-popover.tsx, src/routes/admin/calendar.tsx</files>
  <read_first>src/lib/booking/admin-fns.server.ts, src/lib/timezone/datetime.ts, src/routes/admin.tsx</read_first>
  <action>1. Create `src/components/admin/calendar-event-popover.tsx`:
   - Popover / modal dialog displaying selected calendar item details:
     - For bookings: Requester name, organization, contact, attendance, time slot (WIB), purpose, status badge, and "Buka di Antrean Booking" action shortcut.
     - For closures: Closure reason, date range, affected asset, and note.
2. Create `src/components/admin/admin-calendar-view.tsx`:
   - Header controls:
     - Asset selector dropdown (filter by specific Room or Dormitory, or "Semua Aset").
     - View switch: "Bulan" (Month Grid) vs. "Minggu" (Weekly Time Grid).
     - Navigation buttons: "Bulan Lalu", "Bulan Ini / Hari Ini", "Bulan Depan" with current month/year label.
     - Legend indicator: Hijau (Disetujui), Kuning/Striped (Pending), Merah (Ditutup).
   - Month View: Calendar matrix displaying events per day cell with truncate badges.
   - Week View: 7-column time grid (07:00 - 21:00 WIB for rooms, multi-day bars for dormitories).
   - Interactive click handler opening `CalendarEventPopover`.
3. Create `src/routes/admin/calendar.tsx`:
   - Route for `/admin/calendar` integrating `AdminCalendarView`.
   - Pre-fetches active assets and calendar events using `getAdminCalendarEventsFn`.</action>
  <verify>& "C:\nvm4w\nodejs\node.exe" -e "import('./src/routes/admin/calendar.tsx').then(() => console.log('Calendar route verified'))"</verify>
  <acceptance_criteria>
    - `/admin/calendar` renders Month and Week views with asset filtering.
    - Approved, Pending, and Closure slots are rendered in distinct colors.
    - Clicking any slot opens the event detail popover.
  </acceptance_criteria>
  <done>src/routes/admin/calendar.tsx and calendar components created</done>
</task>

<task type="auto">
  <name>Task 2: Implement Audit History Table & Visual State Diff Viewer</name>
  <files>src/components/admin/audit-diff-viewer.tsx, src/components/admin/audit-table.tsx, src/routes/admin/audit.tsx</files>
  <read_first>src/lib/audit/admin-fns.server.ts, src/lib/audit/audit.server.ts, src/routes/admin.tsx</read_first>
  <action>1. Create `src/components/admin/audit-diff-viewer.tsx`:
   - Visual metadata formatter component:
     - Displays state transitions cleanly (e.g. `Status: pending -> approved`, `Reason: "..."`, `Actor: admin@example.com`, `Timestamp`).
     - Includes an expandable "Lihat JSON Mentah" code block for raw metadata inspection.
2. Create `src/components/admin/audit-table.tsx`:
   - Filter bar: Action type selector (`booking.create`, `booking.approve`, `booking.reject`, `booking.cancel`, `asset.create`, `asset.update`, `user.update`, etc.), Entity type selector (`booking`, `asset`, `user`), Actor search input, Date range filter.
   - Table columns: Waktu (WIB), Aktor, Aksi, Entitas / ID, Ringkasan Perubahan, Detail.
   - Expandable row or drawer to inspect `AuditDiffViewer`.
   - Pagination controls (Next/Prev).
3. Create `src/routes/admin/audit.tsx`:
   - Route for `/admin/audit` integrating `AuditTable`.
   - Calls `getAdminAuditLogsFn` with active filter params.</action>
  <verify>& "C:\nvm4w\nodejs\node.exe" -e "import('./src/routes/admin/audit.tsx').then(() => console.log('Audit route verified'))"</verify>
  <acceptance_criteria>
    - `/admin/audit` displays system audit events with multi-criteria filtering.
    - State transitions and reasons are formatted into readable diffs with expandable raw JSON.
  </acceptance_criteria>
  <done>src/routes/admin/audit.tsx and audit components created</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `& "C:\nvm4w\nodejs\node.exe" --import tsx --test src/lib/booking/admin.test.ts` passes.
- [ ] `& "C:\nvm4w\nodejs\node.exe" --import tsx --test src/db/migration.test.ts src/db/auth.test.ts src/lib/booking/booking.test.ts src/lib/booking/admin.test.ts` passes.
</verification>

<success_criteria>
- Administrators have an asset-centric calendar displaying real-time booking schedules and maintenance closures.
- Full audit history is searchable, filterable, and presents formatted state diffs.
- All Phase 5 requirements (FLOW-02, FLOW-03, OPS-01, OPS-02, OPS-04) are fully operational and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/05-administrative-decisions-operations/05-03-SUMMARY.md`
</output>
