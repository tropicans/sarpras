---
phase: 01-canonical-data-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle.config.ts
  - package.json
  - .env
  - .env.example
  - src/db/schema.ts
  - src/db/client.server.ts
  - src/db/migrate.ts
  - src/db/migrate-legacy.ts
  - src/db/migration.test.ts
  - legacy-data/legacy_assets.json
  - legacy-data/legacy_admins.json
  - legacy-data/legacy_bookings.json
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
user_setup: []
must_haves:
  truths:
    - "An administrator can run the legacy import repeatedly without duplicating records and with preserved source identifiers."
    - "An administrator can review a reconciliation report showing source, imported, rejected, and exception counts without credential exposure."
    - "Migrated booking history remains linked to its migrated asset and requester data."
    - "Every persisted booking date-time records its explicit Asia/Jakarta interpretation and normalized value."
  artifacts:
    - "src/db/schema.ts"
    - "src/db/migrate-legacy.ts"
    - "src/db/migration.test.ts"
  key_links:
    - "src/db/migrate-legacy.ts references src/db/schema.ts"
---

<objective>
Establish the durable PostgreSQL database schemas (using Drizzle ORM) and implement a repeatable, reconcilable CLI migration script to import legacy assets, booking history, and administrator credentials from local JSON files.

Purpose: Durable data foundation, timezone semantics, and repeatable legacy data import.
Output: Drizzle schemas, migrations, CLI importer script, mock data, and integration tests.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup dependencies and configuration files</name>
  <files>package.json, drizzle.config.ts, .env, .env.example</files>
  <action>Install Drizzle and PG dependencies and configure drizzle.config.ts and dotenv variables.</action>
  <verify>npm list drizzle-orm pg</verify>
  <acceptance_criteria>
    - drizzle.config.ts exists and points to ./src/db/schema.ts
  </acceptance_criteria>
  <done>package.json has drizzle-orm dependency</done>
</task>

<task type="auto">
  <name>Task 2: Implement database schema and client</name>
  <files>src/db/schema.ts, src/db/client.server.ts, src/db/migrate.ts</files>
  <action>Create database schemas for users, accounts, sessions, assets, bookings, and audit logs using Drizzle. Configure DB client and migration runner.</action>
  <verify>node src/db/migrate.ts</verify>
  <acceptance_criteria>
    - schema.ts contains table definitions
  </acceptance_criteria>
  <done>src/db/schema.ts exists</done>
</task>

<task type="auto">
  <name>Task 3: Implement legacy CLI importer script</name>
  <files>src/db/migrate-legacy.ts</files>
  <action>Implement legacy data parser and importer using Zod for validation and date-fns-tz for Asia/Jakarta timezone normalization. Implement CLI reconciliation ASCII report.</action>
  <verify>node src/db/migrate-legacy.ts</verify>
  <acceptance_criteria>
    - migrate-legacy.ts contains reconciliation report printing logic
  </acceptance_criteria>
  <done>src/db/migrate-legacy.ts exists</done>
</task>

<task type="auto">
  <name>Task 4: Create mock legacy data and verify</name>
  <files>legacy-data/legacy_assets.json, legacy-data/legacy_admins.json, legacy-data/legacy_bookings.json</files>
  <action>Create mock JSON files and run the legacy migration. Verify idempotency and validation errors.</action>
  <verify>npm run db:migrate-legacy</verify>
  <done>Mock data files exist</done>
</task>

<task type="auto">
  <name>Task 5: Write and run automated tests</name>
  <files>src/db/migration.test.ts</files>
  <action>Write test suite validating timezone interpretation, idempotency, and error tolerance.</action>
  <verify>npm test</verify>
  <done>node --import tsx --test src/db/migration.test.ts runs successfully</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --import tsx --test src/db/migration.test.ts passes without errors
- [ ] npm run db:migrate-legacy runs successfully
</verification>

<success_criteria>
- All database schemas are successfully created in postgres.
- CLI migration script successfully processes legacy records with idempotency.
- Timezone WIB dates are normalized to UTC but preserve WIB display string.
- Tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-data-migration/01-01-SUMMARY.md`
</output>
