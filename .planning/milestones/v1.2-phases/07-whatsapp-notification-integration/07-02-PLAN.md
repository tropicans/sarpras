---
phase: 07-whatsapp-notification-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/lib/booking/service.server.ts
  - src/lib/booking/types.ts
  - src/lib/booking/booking.test.ts
autonomous: true
requirements:
  - WA-04
  - WA-05
  - WA-06
  - WA-07
  - WA-08
user_setup: []

must_haves:
  truths:
    - "Booking creation triggers WhatsApp submission confirmation to requester and operational alert to admin."
    - "Booking approval triggers WhatsApp approval notification with asset and schedule details to requester."
    - "Booking rejection triggers WhatsApp rejection notification with reason to requester."
    - "Booking cancellation triggers operational alert to admin."
    - "All notification dispatches are asynchronous, non-blocking, and executed outside transaction locks."
  artifacts:
    - src/lib/booking/service.server.ts
    - src/lib/booking/booking.test.ts
  key_links:
    - "src/lib/booking/service.server.ts calls safeDispatchNotification from src/lib/whatsapp/service.server.ts"
    - "src/lib/booking/service.server.ts formats messages using generators from src/lib/whatsapp/templates.ts"
---

<objective>
Integrate WhatsApp notification triggers into the core booking lifecycle within `BookingService` (`createBookingRequest`, `approveBooking`, `rejectBooking`, `cancelBooking`) as non-blocking post-commit side-effects, and add comprehensive automated test suites for end-to-end notification flows.

Purpose: Deliver real-time communication to requesters and administrators across all booking status events while preserving database transaction isolation and performance.
Output: Integrated `src/lib/booking/service.server.ts`, updated `src/lib/booking/types.ts`, and full integration test coverage in `src/lib/booking/booking.test.ts`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-whatsapp-notification-integration/07-CONTEXT.md
@.planning/phases/07-whatsapp-notification-integration/07-RESEARCH.md
@src/lib/booking/service.server.ts
@src/lib/whatsapp/service.server.ts
@src/lib/whatsapp/templates.ts
@src/lib/whatsapp/phone.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Notification Hooks into BookingService Lifecycle</name>
  <files>src/lib/booking/service.server.ts, src/lib/booking/types.ts</files>
  <read_first>src/lib/booking/service.server.ts, src/lib/whatsapp/service.server.ts, src/lib/whatsapp/templates.ts</read_first>
  <action>
1. In `src/lib/booking/service.server.ts`:
   - Import `safeDispatchNotification` from `../whatsapp/service.server`.
   - Import message builders from `../whatsapp/templates`.
   - In `createBookingRequest`: After transaction commit, trigger:
     1. Requester confirmation if `input.requesterPhone` is present (`buildBookingSubmissionRequesterMessage`).
     2. Admin operational alert to `process.env.FONNTE_ADMIN_TARGET` (`buildBookingSubmissionAdminMessage`).
   - In `approveBooking`: After transaction commit, query requester details / asset details and trigger `buildBookingApprovalMessage` to `booking.requesterPhone`.
   - In `rejectBooking`: After transaction commit, trigger `buildBookingRejectionMessage` with `rejectionReason` to `booking.requesterPhone`.
   - In `cancelBooking` / `cancelBookingByPublicReference`: After transaction commit, trigger cancellation notifications.
   - Enforce asynchronous, non-blocking pattern: fire dispatch functions without awaiting them inside the transaction or use `void safeDispatchNotification(...)` to ensure DB transaction response times remain instantaneous.
2. In `src/lib/booking/types.ts`: Ensure `CreateBookingInputSchema` accepts sanitized phone strings and provides clear validation errors for malformed phone inputs.
  </action>
  <verify>node --import tsx --test src/lib/booking/booking.test.ts</verify>
  <acceptance_criteria>
    - `BookingService.createBookingRequest` dispatches requester confirmation (WA-04) and admin operational alert (WA-07, WA-08).
    - `BookingService.approveBooking` dispatches approval message (WA-05).
    - `BookingService.rejectBooking` dispatches rejection message with reason (WA-06).
    - Network dispatches are executed post-commit and never block or fail database transactions.
  </acceptance_criteria>
  <done>Booking service lifecycle methods cleanly trigger asynchronous WhatsApp notifications.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-End Test Suite for WhatsApp Notification Integration</name>
  <files>src/lib/booking/booking.test.ts</files>
  <read_first>src/lib/booking/booking.test.ts, src/lib/whatsapp/service.server.ts</read_first>
  <action>
1. Update `src/lib/booking/booking.test.ts` to add a dedicated test suite `Wave 7: WhatsApp Notification & Integration Triggers`:
   - Test booking submission triggers submission confirmation and admin operational alert, with audit logs recorded in `audit_logs` table (`action: "notification.whatsapp_dispatch"`).
   - Test booking approval triggers approval WhatsApp message with asset details.
   - Test booking rejection triggers rejection WhatsApp message with the exact `rejectionReason`.
   - Test that invalid phone numbers or gateway failures do not throw errors or rollback booking state.
2. Run full test suite: `pnpm test`.
  </action>
  <verify>pnpm test</verify>
  <acceptance_criteria>
    - `src/lib/booking/booking.test.ts` includes comprehensive assertions for WhatsApp dispatches and audit log persistence.
    - All tests in `pnpm test` pass with 0 errors.
  </acceptance_criteria>
  <done>Automated test suite validates all WhatsApp notification requirements (WA-01 to WA-08) end-to-end.</done>
</task>

</tasks>
