# Quick Task: 260818-hu1 - Rapihkan Directory Root & Manage File Root

## Objective
Membersihkan dan merapihkan root direktori proyek dengan memindahkan file non-konfigurasi root ke lokasi yang tepat (scripts/ dan docs/reports/), menghapus file scratch sementara, serta memastikan root bersih dari file artefak liar.

## Tasks
1. **Pindahkan Skrip & Artefak Laporan ke Folder Terstruktur:**
   - Buat folder `scripts/` (jika belum ada) dan pindahkan `generate_report_pdf.py` ke `scripts/generate_report_pdf.py`.
   - Buat folder `docs/reports/` dan pindahkan `197906192008011012_20260814.pdf` serta `Laporan_Harian_Teknikal_SARPRAS_2026-08-14.html` ke `docs/reports/`.
2. **Hapus File Scratch/Temporary:**
   - Hapus `scratch_hooks.json` dari root direktori.
3. **Pembaruan Path & .gitignore:**
   - Sesuaikan path output di `scripts/generate_report_pdf.py` agar mengarah ke `docs/reports/`.
   - Update `.gitignore` untuk mengabaikan file scratch sementara `scratch_*.json` dan file output temporary.
4. **Verifikasi:**
   - Jalankan `git status` dan `npm test` untuk memastikan semua integrasi dan pengujian tetap berjalan sempurna.
