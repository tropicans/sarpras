---
phase: 02-secure-administration-asset-setup
plan: 02
type: execute
wave: 2
depends_on: ["02-01-PLAN.md"]
files_modified:
  - src/routes/login.tsx
  - src/routes/admin.tsx
  - src/routes/admin/users.tsx
  - src/db/auth.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - "D-03: Bridging legacy passwords: Require migrated administrator accounts to perform a password reset flow upon their first sign-in."
    - "D-13: Deactivate users by immediately deleting all their session records from the database session table to enforce immediate logouts across all devices."
    - "D-15: Record account deactivation and session revocation events in the audit_logs table."
    - "D-16: Admin users with the 'admin' role trigger user deactivation through a secure user management UI page calling a protected server function."
  artifacts:
    - "src/routes/login.tsx"
    - "src/routes/admin.tsx"
    - "src/routes/admin/users.tsx"
  key_links:
    - "src/routes/admin/users.tsx references src/lib/auth.middleware.ts"
---

<objective>
Implement the administrative client interfaces and account actions. Build the Login Page, the authenticated Admin layout route with sidebar navigation, the User Management listing, and the user deactivation session-revocation flow.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-secure-administration-asset-setup/02-CONTEXT.md
@.planning/phases/02-secure-administration-asset-setup/02-UI-SPEC.md
@.planning/phases/02-secure-administration-asset-setup/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Login Page with forced password reset check</name>
  <files>src/routes/login.tsx</files>
  <read_first>src/lib/auth-client.ts</read_first>
  <action>Create src/routes/login.tsx rendering the centered isomorphic Login Card. Handle form submission via authClient.signIn.email. In beforeLoad or right after login, check if mustResetPassword is true; if so, render the forced password reset fields instead of standard dashboard access. Support loading states and the UI-SPEC login error text.</action>
  <verify>npm run generate-routes</verify>
  <acceptance_criteria>
    - login.tsx contains centered Card form element.
    - Submit button shows loading spinner.
    - Incorrect login renders 'Could not authenticate. Please check your email and password and try again.' error.
  </acceptance_criteria>
  <done>src/routes/login.tsx is created</done>
</task>

<task type="auto">
  <name>Task 2: Build Admin layout and sidebar navigation</name>
  <files>src/routes/admin.tsx</files>
  <read_first>src/lib/auth.middleware.ts</read_first>
  <action>Create src/routes/admin.tsx (or layout router route) with beforeLoad protecting it: call getSession, if null throw redirect to /login. If session exists but mustResetPassword is true, throw redirect to /login?reset=true. In layout component, render the Admin Sidebar with Lucide icons (Users, Assets) and the Main content area.</action>
  <verify>npm run generate-routes</verify>
  <acceptance_criteria>
    - admin.tsx protects sub-routes via beforeLoad session check.
    - admin.tsx renders sidebar navigation links.
  </acceptance_criteria>
  <done>src/routes/admin.tsx is created</done>
</task>

<task type="auto">
  <name>Task 3: Build User Management page and deactivation actions</name>
  <files>src/routes/admin/users.tsx</files>
  <read_first>src/lib/auth.middleware.ts, src/db/schema.ts</read_first>
  <action>Create src/routes/admin/users.tsx rendering a paginated User Accounts Table. Write a protected server function (using authMiddleware) to retrieve the admin list and another server function to deactivate a user: set user status to 'inactive', delete sessions where userId matches the target user, and record an audit log with actor and target user info.</action>
  <verify>npm run generate-routes</verify>
  <acceptance_criteria>
    - users.tsx defines paginated list of users.
    - Deactivate server function executes Drizzle queries to deactivate user and delete user session rows.
    - Deactivation writes audit log entry.
  </acceptance_criteria>
  <done>src/routes/admin/users.tsx is created</done>
</task>

<task type="auto">
  <name>Task 4: Write tests for password reset and deactivation flows</name>
  <files>src/db/auth.test.ts</files>
  <read_first>src/db/auth.test.ts, src/routes/admin/users.tsx</read_first>
  <action>Add tests to src/db/auth.test.ts verifying: 1) deactivating a user updates status to 'inactive' in user table, 2) deactivation successfully deletes all session rows for that user in session table, 3) deactivation produces audit log in audit_logs table, 4) authMiddleware immediately rejects a user whose status is 'inactive'.</action>
  <verify>node --import tsx --test src/db/auth.test.ts</verify>
  <acceptance_criteria>
    - Tests verify session rows deletion and audit logs writing.
    - Tests run successfully.
  </acceptance_criteria>
  <done>node --import tsx --test src/db/auth.test.ts runs successfully</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --import tsx --test src/db/auth.test.ts runs and passes
- [ ] npm run generate-routes runs and updates route tree
</verification>

<success_criteria>
- Login Page renders correctly with loading/error states.
- Admin layout guards all children routes behind session validation.
- User management table lists admin accounts with status and roles.
- Deactivating a user immediately deletes session rows in the database, blocking subsequent access at the middleware level, and writes audit records.
- Tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-secure-administration-asset-setup/02-02-SUMMARY.md`
</output>
