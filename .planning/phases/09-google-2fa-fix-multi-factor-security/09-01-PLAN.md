---
phase: 09-google-2fa-fix-multi-factor-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/auth.server.ts
  - src/components/admin/two-factor-setup-modal.tsx
  - src/routes/two-factor.tsx
  - src/lib/auth/two-factor-enable-bug.test.ts
  - src/lib/auth/two-factor.test.ts
autonomous: true
requirements:
  - 2FA-01
  - 2FA-02
  - 2FA-03
  - 2FA-04
  - 2FA-05
user_setup: []
must_haves:
  truths:
    - "Google OAuth and passwordless users can initiate 2FA activation without encountering 400 Bad Request or Invalid password errors"
    - "User can view TOTP QR Code, manual secret key, enter 6-digit confirmation code, and receive backup codes"
    - "User can safely disable 2FA from the modal without requiring a password"
    - "User with 2FA enabled is challenged on /two-factor during login and can verify via TOTP code or backup code"
    - "Automated reproduction and lifecycle test suites pass cleanly"
  artifacts:
    - path: "src/db/auth.server.ts"
      provides: "Better Auth server configuration with twoFactor plugin allowPasswordless: true"
      contains: "allowPasswordless: true"
    - path: "src/components/admin/two-factor-setup-modal.tsx"
      provides: "2FA setup and management modal for admin portal"
      contains: "authClient as any).twoFactor"
    - path: "src/routes/two-factor.tsx"
      provides: "Login 2FA verification challenge route"
      contains: "verifyTotp"
    - path: "src/lib/auth/two-factor-enable-bug.test.ts"
      provides: "Automated reproduction and regression test for passwordless 2FA enablement"
      contains: "allowPasswordless"
    - path: "src/lib/auth/two-factor.test.ts"
      provides: "Automated schema and configuration tests for 2FA"
      contains: "twoFactors"
  key_links:
    - from: "src/components/admin/two-factor-setup-modal.tsx"
      to: "src/db/auth.server.ts"
      via: "Calls Better Auth twoFactor endpoints (enable, verifyTotp, disable)"
      pattern: "twoFactor\\.(enable|verifyTotp|disable)"
    - from: "src/routes/two-factor.tsx"
      to: "src/db/auth.server.ts"
      via: "Calls Better Auth twoFactor verifyTotp and verifyBackupCode"
      pattern: "twoFactor\\.(verifyTotp|verifyBackupCode)"
---

<objective>
Deliver the complete fix and verification for Two-Factor Authentication (TOTP) supporting Google OAuth and passwordless accounts.

Purpose: Prevent 400 Bad Request / "Invalid password" errors during 2FA activation, provide seamless setup & backup codes, safe disabling, and reliable login challenge flows.
Output: Validated Better Auth server configuration, robust setup modal, 2FA challenge route, and automated reproduction & lifecycle test suite.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-google-2fa-fix-multi-factor-security/09-RESEARCH.md
@.planning/phases/09-google-2fa-fix-multi-factor-security/09-VALIDATION.md

@src/db/auth.server.ts
@src/components/admin/two-factor-setup-modal.tsx
@src/routes/two-factor.tsx
@src/lib/auth/two-factor-enable-bug.test.ts
@src/lib/auth/two-factor.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and Harden Better Auth Server 2FA Configuration</name>
  <files>src/db/auth.server.ts</files>
  <read_first>src/db/auth.server.ts, src/db/schema.ts</read_first>
  <action>
    Ensure `auth.server.ts` registers `twoFactor` with:
    - `issuer: "SARPRAS PPKASN"`
    - `allowPasswordless: true`
    - `drizzleAdapter` mapping `twoFactor: schema.twoFactors` and `user: schema.users`
    - Account linking enabled for trusted provider `"google"`
  </action>
  <verify>
    node --import tsx --test src/lib/auth/two-factor-enable-bug.test.ts
  </verify>
  <acceptance_criteria>
    - `src/db/auth.server.ts` contains `allowPasswordless: true` in `twoFactor` plugin configuration
    - `src/db/auth.server.ts` contains `issuer: "SARPRAS PPKASN"`
    - `src/db/schema.ts` includes `twoFactors` table export and `twoFactorEnabled` on `users`
  </acceptance_criteria>
  <done>Server configuration supports passwordless 2FA enablement without 400 Bad Request error</done>
</task>

<task type="auto">
  <name>Task 2: Audit 2FA Setup Modal and Login Challenge Route UX</name>
  <files>src/components/admin/two-factor-setup-modal.tsx, src/routes/two-factor.tsx</files>
  <read_first>src/components/admin/two-factor-setup-modal.tsx, src/routes/two-factor.tsx, src/lib/auth-client.ts</read_first>
  <action>
    1. In `TwoFactorSetupModal`:
       - Ensure `authClient.twoFactor.enable({})` and `authClient.twoFactor.disable({})` pass `{}` cleanly without prompting or requiring password for OAuth users.
       - Verify QR code generation via `totpURI` and manual secret string fallback.
       - Ensure backup codes are displayed, copyable with feedback indicator, and clearly warned as one-time emergency codes.
       - Handle status updates via `onStatusChange(true/false)`.
    2. In `TwoFactorVerificationComponent` (`src/routes/two-factor.tsx`):
       - Support 6-digit TOTP verification via `authClient.twoFactor.verifyTotp({ code })`.
       - Support emergency backup code verification via `authClient.twoFactor.verifyBackupCode({ code })`.
       - On success, navigate to `/admin`.
  </action>
  <verify>
    node --import tsx --test src/lib/auth/two-factor.test.ts
  </verify>
  <acceptance_criteria>
    - `src/components/admin/two-factor-setup-modal.tsx` contains `authClient.twoFactor.enable` and `authClient.twoFactor.disable`
    - `src/routes/two-factor.tsx` contains both `verifyTotp` and `verifyBackupCode` methods
    - Modal provides copy secret and copy backup codes with instant user visual feedback
  </acceptance_criteria>
  <done>Client setup modal and login challenge page handle 2FA lifecycle cleanly for OAuth users</done>
</task>

<task type="auto">
  <name>Task 3: Automated Test Suite & Regression Verification</name>
  <files>src/lib/auth/two-factor-enable-bug.test.ts, src/lib/auth/two-factor.test.ts</files>
  <read_first>src/lib/auth/two-factor-enable-bug.test.ts, src/lib/auth/two-factor.test.ts</read_first>
  <action>
    1. Verify `src/lib/auth/two-factor-enable-bug.test.ts` asserts that `allowPasswordless` is set to `true` on the `two-factor` plugin options.
    2. Verify `src/lib/auth/two-factor.test.ts` validates plugin registration, issuer, database schema tables (`twoFactors`), and `twoFactorEnabled` user column.
    3. Run both tests and ensure 100% pass rate.
  </action>
  <verify>
    node --import tsx --test src/lib/auth/two-factor-enable-bug.test.ts src/lib/auth/two-factor.test.ts
  </verify>
  <acceptance_criteria>
    - All 2FA unit and regression tests pass without errors or unhandled rejections
    - Test execution completes in < 5 seconds
  </acceptance_criteria>
  <done>Automated tests confirm bug fix and complete 2FA lifecycle integrity</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --import tsx --test src/lib/auth/two-factor-enable-bug.test.ts src/lib/auth/two-factor.test.ts` passes
- [ ] `npm test` passes
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No 400 Bad Request error when enabling 2FA for Google OAuth accounts
- Requirements 2FA-01, 2FA-02, 2FA-03, 2FA-04, and 2FA-05 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/09-google-2fa-fix-multi-factor-security/09-01-SUMMARY.md`
</output>
