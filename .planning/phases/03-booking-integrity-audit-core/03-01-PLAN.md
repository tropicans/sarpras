---
phase: 03-booking-integrity-audit-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/booking/types.ts
  - src/lib/booking/state-machine.ts
  - src/lib/timezone/datetime.ts
  - src/lib/booking/availability.ts
  - src/lib/booking/booking.test.ts
autonomous: true
requirements:
  - FLOW-01
  - FLOW-05
user_setup: []
must_haves:
  truths:
    - "D-03: Normalize all input dates to UTC timestamps at the database boundary, and interpret them in Asia/Jakarta when validating operating hours and closures."
    - "D-04: Overlap Blocking Policy: Only block overlapping APPROVED bookings. Multiple pending requests for the same slot are allowed to coexist until one is approved."
    - "D-05: Enforce a strict standard lifecycle where pending can transition to approved, rejected, or cancelled, and approved can transition to cancelled. Rejected and cancelled are terminal states."
    - "D-06: Require a non-empty string in the rejectionReason column when transitioning a booking's status to 'rejected'."
    - "D-10: Room bookings are strictly exclusive (one booking at a time) and must have requested attendance less than or equal to the room capacity."
    - "D-11: Dormitories skip daily open/close hours (they operate 24/7), but must validate against closures/holidays. Rooms validate against both daily operating hours and closure dates."
  artifacts:
    - "src/lib/booking/types.ts"
    - "src/lib/booking/state-machine.ts"
    - "src/lib/timezone/datetime.ts"
    - "src/lib/booking/availability.ts"
    - "src/lib/booking/booking.test.ts"
  key_links:
    - "src/lib/booking/availability.ts references src/lib/timezone/datetime.ts"
    - "src/lib/booking/state-machine.ts references src/lib/booking/types.ts"
---

<objective>
Build the core state machine, timezone date normalization engine, and authoritative availability validator for rooms and dormitories.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-booking-integrity-audit-core/03-CONTEXT.md
@.planning/phases/03-booking-integrity-audit-core/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Booking Domain Types and Schemas</name>
  <files>src/lib/booking/types.ts</files>
  <read_first>src/db/schema.ts</read_first>
  <action>Create `src/lib/booking/types.ts` defining TypeScript types and Zod schemas for: `BookingStatus` ('pending' | 'approved' | 'rejected' | 'cancelled'), `AssetType` ('room' | 'dormitory'), transition payloads (including optional/required `rejectionReason`), booking creation inputs, availability query intervals, and conflict result types.</action>
  <verify>npx tsx -e "import { BookingStatusSchema } from './src/lib/booking/types'; console.log('Types loaded:', !!BookingStatusSchema)"</verify>
  <acceptance_criteria>
    - `BookingStatusSchema` validates 'pending', 'approved', 'rejected', 'cancelled'.
    - Transition schemas enforce `rejectionReason` string for 'rejected' status.
  </acceptance_criteria>
  <done>src/lib/booking/types.ts is created</done>
</task>

<task type="auto">
  <name>Task 2: Implement Booking State Machine</name>
  <files>src/lib/booking/state-machine.ts</files>
  <read_first>src/lib/booking/types.ts, .planning/phases/03-booking-integrity-audit-core/03-CONTEXT.md</read_first>
  <action>Create `src/lib/booking/state-machine.ts` implementing: `validateBookingTransition(currentStatus, nextStatus, rejectionReason?)` and `canTransition(currentStatus, nextStatus)`. Enforce: `pending` -> `approved` | `rejected` | `cancelled`; `approved` -> `cancelled`; `rejected` -> [] (terminal); `cancelled` -> [] (terminal). Throw descriptive errors or return structured validation failures when invalid transitions are attempted or when rejection lacks a non-empty `rejectionReason` (D-05, D-06).</action>
  <verify>npx tsx -e "import { validateBookingTransition } from './src/lib/booking/state-machine'; validateBookingTransition('pending', 'approved'); console.log('State machine valid')"</verify>
  <acceptance_criteria>
    - `validateBookingTransition('pending', 'approved')` succeeds.
    - `validateBookingTransition('pending', 'rejected', '')` throws error requiring rejection reason.
    - `validateBookingTransition('rejected', 'approved')` throws error (terminal state).
    - `validateBookingTransition('cancelled', 'approved')` throws error (terminal state).
  </acceptance_criteria>
  <done>src/lib/booking/state-machine.ts is created with full transition rules</done>
</task>

<task type="auto">
  <name>Task 3: Implement Asia/Jakarta Timezone Normalization and Operating Availability Engine</name>
  <files>src/lib/timezone/datetime.ts, src/lib/booking/availability.ts</files>
  <read_first>src/db/schema.ts, .planning/phases/03-booking-integrity-audit-core/03-RESEARCH.md</read_first>
  <action>Create `src/lib/timezone/datetime.ts` using `date-fns` and `date-fns-tz` to normalize dates to UTC at database boundaries and convert UTC timestamps to `Asia/Jakarta` (WIB, UTC+7) for wall-clock evaluation. Create `src/lib/booking/availability.ts` to validate:
1. Operating hours for rooms: check if the booking's start and end times in `Asia/Jakarta` fall within the asset's configured `openTime` and `closeTime` for the corresponding day of the week (D-03, D-11).
2. Closure validation: ensure neither start nor end dates fall on dates configured in `assetClosures` (applies to both rooms and dormitories).
3. Room overlap check: query existing approved bookings for the room and verify that `[startDate, endDate]` does not overlap with any existing approved booking (D-04, D-10).
4. Capacity check: ensure requested attendance does not exceed asset capacity (D-10).</action>
  <verify>npx tsx -e "import { toJakartaDate } from './src/lib/timezone/datetime'; import { checkRoomAvailability } from './src/lib/booking/availability'; console.log('Availability helpers loaded')"</verify>
  <acceptance_criteria>
    - `src/lib/timezone/datetime.ts` exports timezone conversion utilities with `Asia/Jakarta` constant.
    - `src/lib/booking/availability.ts` correctly validates operating hours, closures, and room overlaps.
  </acceptance_criteria>
  <done>src/lib/timezone/datetime.ts and src/lib/booking/availability.ts are implemented</done>
</task>

<task type="auto">
  <name>Task 4: Write Unit Tests for State Machine and Availability Logic</name>
  <files>src/lib/booking/booking.test.ts</files>
  <read_first>src/lib/booking/state-machine.ts, src/lib/booking/availability.ts</read_first>
  <action>Create `src/lib/booking/booking.test.ts` using `node:test` and `node:assert`. Add tests for:
1. State machine valid transitions (`pending` -> `approved`, `pending` -> `rejected` with reason, `approved` -> `cancelled`).
2. State machine illegal transitions (`rejected` -> `approved`, `cancelled` -> `pending`, `pending` -> `rejected` without reason).
3. Timezone conversion and boundary calculations in `Asia/Jakarta`.
4. Room operating hours validation (within hours vs outside hours) and closure day matching.</action>
  <verify>node --import tsx --test src/lib/booking/booking.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/booking/booking.test.ts` executes and all tests pass.
  </acceptance_criteria>
  <done>Unit tests pass cleanly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --import tsx --test src/lib/booking/booking.test.ts` runs and passes
</verification>

<success_criteria>
- State machine transition rules enforced with rejection reason requirement.
- Timezone normalization and availability engine validates operating hours, closures, and room exclusivity.
- Unit tests cover all transition branches and availability edge cases.
</success_criteria>

<output>
After completion, create `.planning/phases/03-booking-integrity-audit-core/03-01-SUMMARY.md`
</output>
